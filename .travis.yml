language: java

sudo: required

before_install:
 - chmod +x gradlew
 - sudo apt-get -qq update
 - sudo apt-get install -y libstdc++6
 - sudo updatedb
 - locate libstdc++.so.6 
 - sudo ln -sf /usr/lib/x86_64-linux-gnu/libstdc++.so.6.0.16 /home/travis/.m2/repository/webdriver/chromedriver/linux64/2.29/chromedriver/libstdc++.so.6

 
after_success:
- ./gradlew jacocoTestReport coveralls